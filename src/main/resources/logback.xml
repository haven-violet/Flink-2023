<?xml version="1.0" encoding="UTF-8"?>
<configuration>
<!--
Appender: 设置日志信息的去向, 常用的有以下几个
ch.qos.logback.core.consoleAppender (控制台)
ch.qos.logback.core.rolling.RollingFileAppender (文件大小到达指定尺寸的时候生成一个新文件)
ch.qos.logback.core.FileAppender (文件)
-->
    <!--
     用来设置某一个包或者具体的某一个类的日志打印级别、以及指定<appender>
     <loger>仅有一个name属性，一个可选的level和一个可选的addtivity属性
     name: 用来指定受此logger约束的某一个包或者具体的某一个类
     level: 用来设置打印级别，大小写无关：TRACE, DEBUG, INFO, WARN, ERROR, ALL 和 OFF
     如果未设置此属性，那么当前logger将会继承上级的级别
     additivity:是否向上级loger传递打印信息。默认是true
     <logger>可以包含零个或多个<appender-ref>元素，标识这个appender将会添加到这个logger
     也是<logger>元素，但是它是根logger。默认debug
     <root>可以包含零个或多个<appender-ref>元素，标识这个appender将会添加到这个logger
     -->
<!--
日志输出格式:
%5level
%d{yyyy-MM-dd HH:mm:ss.SSS} 日期
%c 类的完整名称
%M 为Method
%L 为行号
%thread 线程名称
%m或者%msg为信息
%n 为换行
 -->
    <property name="pattern" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %c [%thread] %-5level %msg%n"/>
    <property name="log_dir" value="E:\\workspace\\data"></property>
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target> <!--输出流对象默认为System.out 改为System.err -->
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${pattern}</pattern>
        </encoder>
    </appender>
    <appender name="file" class="ch.qos.logback.core.FileAppender">
    `   <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder"> <!-- 日志格式配置-->
            <pattern>${pattern}</pattern>
        </encoder>
        <file>${log_dir}/loback.log</file>  <!--日志输出路径-->
    </appender>
    <appender name="htmlFile" class="ch.qos.logback.core.FileAppender" >
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="ch.qos.logback.classic.html.HTMLLayout">
                <pattern>%level%d{yyyy-MM-dd HH:mm:ss}%c%M%L%thread%m</pattern>
            </layout>
        </encoder>
        <file>${log_dir}/logback.html</file>
    </appender>

    <root level="ALL">
        <appender-ref ref="console"/>
        <appender-ref ref="file"/>
        <appender-ref ref="htmlFile"/>
    </root>

</configuration>